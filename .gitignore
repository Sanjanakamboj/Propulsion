# Python
__pycache__/
*.pyc
.pytest_cache/

# Generated run outputs (reproducible from engine_inputs.txt via design_engine.py) --
# ignored by default so every local run doesn't accumulate in git, except
# Results/example_run/, which is committed as a reference sample.
Results/*
!Results/example_run/
design_log.xlsx

# OS
.DS_Store
